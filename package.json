{
  "name": "sh-web",
  "private": true,
  "type": "module",
  "description": "Sudut Haramain — Media Digital & Informasi Praktis Makkah–Madinah",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:ssr": "node --test tests/ssr/*.test.mjs",
    "typecheck": "nuxt prepare && vue-tsc --noEmit",
    "build": "nuxt build",
    "dev": "nuxt dev --host localhost --port 3003",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "lucide-vue-next": "^1.0.0",
    "maplibre-gl": "^6.6.0",
    "nuxt": "3.21.11",
    "vue": "^3.5.13",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.2",
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vue-tsc": "^2.2.12"
  }
}

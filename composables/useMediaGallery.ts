export type MediaGalleryItem={id:number;src:string;alt:string;title:string;city:'makkah'|'madinah';locationName:string;category:string;tags:string[];date:string;description:string;mapUrl?:string;priority:number;publishedAt:string|null;orientation:'portrait'|'landscape'}
function apiUrl(){const c=useRuntimeConfig();return `${String(c.public.mediaApiBaseUrl||'').replace(/\/$/,'')}/api/v1/media/gallery`}
function normalize(r:any):MediaGalleryItem{return {id:r.id,src:r.imageUrl||'',alt:r.altText||r.title||r.locationName||'Visual Haramain',title:r.title||r.locationName||'Visual Haramain',city:String(r.city||'makkah').toLowerCase() as 'makkah'|'madinah',locationName:r.locationName||r.title||'',category:String(r.category||'').toLowerCase().replace(/^./,x=>x.toUpperCase()),tags:Array.isArray(r.tags)?r.tags:[],date:r.publishedAt?new Date(r.publishedAt).toLocaleDateString('id-ID',{day:'2-digit',month:'short',year:'numeric'}):'—',description:r.description||'',mapUrl:r.googleMapsUrl,priority:Number(r.priority||0),publishedAt:r.publishedAt,orientation:'landscape'}}
export async function fetchMediaGallery(query:Record<string,unknown>={}){const r=await $fetch<{data:any[]}>(apiUrl(),{query:{limit:100,...query}});return (r.data||[]).map(normalize)}
export async function useMediaGallery(query:Record<string,unknown>={}){const {data,pending,error}=await useAsyncData(`media-gallery-${JSON.stringify(query)}`,()=>fetchMediaGallery(query),{default:()=>[]});return {items:data,pending,error}}
